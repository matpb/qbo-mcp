node_modules
dist
dist-lambda
*.log
*.mcpb
qbo-mcp-*.zip
.DS_Store

# Host-side Docker volume for OAuth tokens — NEVER commit
data/

# Local credential caches — NEVER commit
.env
.env.*
!.env.example
credentials.json
*.credentials.json
dev-credentials.json

# Per-dev Claude Code MCP config (may hold local paths / env); keep local
.mcp.json
