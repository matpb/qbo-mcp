node_modules
dist
dist-lambda
*.log
.DS_Store

# Host-side Docker volume for OAuth tokens — NEVER commit
data/

# Local credential caches — NEVER commit
.env
.env.*
!.env.example
credentials.json
*.credentials.json
dev-credentials.json
